{
    "typescript.check.workspaceVersion": false,
     "files.exclude": {
        // exclude .js and .js.map files, when in a TypeScript project
        "node_modules": true,
        "e2e": true,
        "**/*.js": { "when": "$(basename).ts"},
        "**/*.js.map": true
    }
    
}